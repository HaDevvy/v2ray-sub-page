services:
  v2-sub-page:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: v2-sub-page
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000"
    volumes:
      - /root/dev/Market/last_ech.txt:/app/last_ech.txt:ro
